{
  "name": "spar-qa-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright end-to-end + accessibility checks for SPAR Fresh Procurement (QA audit; not part of the app build)",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.0",
    "@playwright/test": "^1.49.0"
  }
}
